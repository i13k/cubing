* optional delay between each score shown in `info' and start showing from the bottom
* update dependencies
* random numbers screen !!!,
* use participant ID
* bug [!!]: scoreboard sometimes shows people who have been removed
* bug [!!]: judge phones must always ask for correct number of stages.
* add structure:
  event -> puzzle -> round
// * bug: pages don't work on first load -- probably because of HMR/compilation in dev mode
// * custom Infomessage screen, (drukowanie dyplomów, etc.) -- will be separately created, not as part of this app